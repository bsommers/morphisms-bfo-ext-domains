# How To Use

1. **Install Stack**: `curl -sSL https://get.haskellstack.org/ | sh`
2. **Build**: `stack build`
3. **Run**: `stack exec ontologic-exe -- -i logic/fleet.logic -t python -o .`

## Docker
`docker-compose up`
